{
  "name": "jsdoc-press",
  "version": "1.0.0",
  "description": "A minimal, clean JSDoc HTML template for use via opts.template.",
  "license": "MIT",
  "main": "publish.js",
  "files": [
    "publish.js",
    "tmpl/",
    "static/",
    "README.md"
  ],
  "keywords": [
    "jsdoc",
    "template",
    "docs",
    "documentation"
  ],
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "preview": "jsdoc -c preview.jsdoc.json",
    "preview:watch": "nodemon --watch publish --watch static --watch preview-lib --watch preview-assets --watch tmpl --ext js,md,json,css,tmpl --exec \"npm run preview\""
  },
  "devDependencies": {
    "highlight.js": "^11.11.1",
    "jsdoc": "^4.0.3",
    "nodemon": "^3.1.10"
  }
}
