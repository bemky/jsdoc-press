/**
 * Emits events.
 * @fires Publisher#tick
 */
class Publisher {
  /** Trigger a tick. */
  tick() {}
}

/**
 * Tick event.
 * @event Publisher#tick
 * @type {object}
 * @property {number} count - Tick count.
 */

module.exports = { Publisher };

